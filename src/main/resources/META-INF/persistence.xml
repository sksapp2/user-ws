<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">
    <persistence-unit name="user_db">
        <provider>org.hibernate.reactive.provider.ReactivePersistenceProvider</provider>
        <class>com.infc.ms.user.model.UserModel</class>
        <properties>
            <!-- mysql -->
            <property name="hibernate.jdbc.time_zone" value="UTC"/>
            <property name="javax.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/user_ms?autoReconnect=true"/>
            <!-- Credentials -->
            <property name="javax.persistence.jdbc.user"
                      value="root"/>
            <property name="javax.persistence.jdbc.password"
                      value="root"/>
            <!-- The Vert.x SQL Client connection pool size -->
            <property name="javax.persistence.schema-generation.database.action"
                      value="drop-and-create"/>

            <property name="hibernate.vertx.pool.max_wait_queue_size" value="5"/>
            <property name="hibernate.vertx.pool.connect_timeout" value="500" />
            <property name="hibernate.vertx.pool.idle_timeout" value="200" />
            <property name="hibernate.vertx.pool.cleaner_period" value="30" />
            <property name="hibernate.vertx.prepared_statement_cache.max_size" value="12"/>
            <property name="hibernate.vertx.prepared_statement_cache.sql_limit" value="6"/>
        </properties>
    </persistence-unit>
</persistence>